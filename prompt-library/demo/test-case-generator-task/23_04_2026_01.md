# Test Case Generator Task

## Input Code

```java
import java.time.Duration;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicInteger;
import java.util.function.Function;

public class AsyncCacheExecutor<K, V> {

    private final Map<K, CompletableFuture<V>> cache = new ConcurrentHashMap<>();
    private final ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(1);
    private final ExecutorService executor = Executors.newFixedThreadPool(Runtime.getRuntime().availableProcessors());
    private final int maxRetries;

    public AsyncCacheExecutor(int maxRetries) {
        this.maxRetries = maxRetries;
    }

    public CompletableFuture<V> getOrCompute(
            K key,
            Function<K, V> task,
            Duration timeout
    ) {
        return cache.computeIfAbsent(key, k -> executeWithRetry(k, task, timeout));
    }

    private CompletableFuture<V> executeWithRetry(
            K key,
            Function<K, V> task,
            Duration timeout
    ) {
        AtomicInteger attempt = new AtomicInteger(0);
        CompletableFuture<V> result = new CompletableFuture<>();

        Runnable execution = new Runnable() {
            @Override
            public void run() {
                CompletableFuture
                        .supplyAsync(() -> task.apply(key), executor)
                        .orTimeout(timeout.toMillis(), TimeUnit.MILLISECONDS)
                        .whenComplete((value, error) -> {
                            if (error == null) {
                                result.complete(value);
                            } else if (attempt.incrementAndGet() <= maxRetries) {
                                scheduler.schedule(this, 100L * attempt.get(),
                                        TimeUnit.MILLISECONDS);
                            } else {
                                cache.remove(key);
                                result.completeExceptionally(error);
                            }
                        });
            }
        };

        execution.run();
        return result.thenApply(Objects::requireNonNull);
    }

    public Optional<V> getIfCompleted(K key) {
        return Optional.ofNullable(cache.get(key))
                .filter(CompletableFuture::isDone)
                .filter(f -> !f.isCompletedExceptionally())
                .map(CompletableFuture::join);
    }

    public void shutdown() {
        executor.shutdown();
        scheduler.shutdown();
        cache.clear();
    }
}
```

### Response

```json
{
  "test_cases": [
    {
      "scenario": "Successful task execution and caching",
      "input": "getOrCompute(key='test', task returns 'result', timeout=1000ms)",
      "expected_output": "Returns CompletableFuture<'result'>; subsequent calls with same key return cached future without re-executing task"
    },
    {
      "scenario": "Cache hit - same key, different task function",
      "input": "First call: getOrCompute('key', task1, timeout); Second call: getOrCompute('key', task2, timeout)",
      "expected_output": "Second call returns same cached future as first call; task2 is never executed"
    },
    {
      "scenario": "Task timeout",
      "input": "getOrCompute(key='timeout_test', task sleeps >timeout, timeout=100ms)",
      "expected_output": "CompletableFuture completes exceptionally after timeout; on maxRetries > 0, retries triggered; cache entry removed after max retries"
    },
    {
      "scenario": "Task throws exception on first attempt",
      "input": "getOrCompute(key='error', task throws RuntimeException, timeout=1000ms, maxRetries=2)",
      "expected_output": "Task re-executes with 100ms initial backoff, then 200ms second backoff; if retries exhausted, cache entry removed and exception propagated"
    },
    {
      "scenario": "Zero retries configured",
      "input": "new AsyncCacheExecutor(maxRetries=0); getOrCompute with failing task",
      "expected_output": "Task executes once; on failure, cache entry removed immediately without retry"
    },
    {
      "scenario": "Maximum retries exceeded",
      "input": "getOrCompute with consistently failing task, maxRetries=3",
      "expected_output": "Task executed 4 times total (1 initial + 3 retries); cache entry removed; CompletableFuture completes exceptionally with last error"
    },
    {
      "scenario": "getIfCompleted with completed future",
      "input": "getIfCompleted(key) where key has successful cached result",
      "expected_output": "Returns Optional containing the cached value"
    },
    {
      "scenario": "getIfCompleted with pending future",
      "input": "getIfCompleted(key) where key has in-progress computation",
      "expected_output": "Returns empty Optional (isDone() returns false)"
    },
    {
      "scenario": "getIfCompleted with failed future",
      "input": "getIfCompleted(key) where cached future completed exceptionally",
      "expected_output": "Returns empty Optional (isCompletedExceptionally() returns true)"
    },
    {
      "scenario": "getIfCompleted with non-existent key",
      "input": "getIfCompleted(key) with key never accessed",
      "expected_output": "Returns empty Optional (cache.get returns null)"
    },
    {
      "scenario": "Retry backoff calculation",
      "input": "Task fails 2 times; maxRetries=3; verify scheduling delays",
      "expected_output": "1st retry scheduled at 100ms (100 * 1); 2nd retry scheduled at 200ms (100 * 2)"
    },
    {
      "scenario": "Concurrent getOrCompute calls with same key",
      "input": "Two threads call getOrCompute simultaneously with same key",
      "expected_output": "computeIfAbsent ensures only one task execution; both threads receive same CompletableFuture"
    },
    {
      "scenario": "Concurrent getOrCompute calls with different keys",
      "input": "Multiple threads call getOrCompute with different keys simultaneously",
      "expected_output": "Each key triggers independent task execution concurrently on executor threadpool"
    },
    {
      "scenario": "getOrCompute returns null result",
      "input": "Task execution completes with null value",
      "expected_output": "result.complete(null) called; thenApply(Objects::requireNonNull) throws NullPointerException"
    },
    {
      "scenario": "shutdown() called during active computations",
      "input": "shutdown() while futures are still completing",
      "expected_output": "executor.shutdown() and scheduler.shutdown() initiated; cache.clear() called; pending operations may not complete gracefully"
    },
    {
      "scenario": "Negative maxRetries",
      "input": "new AsyncCacheExecutor(maxRetries=-5); task fails",
      "expected_output": "Condition 'attempt.incrementAndGet() <= -5' always false; no retries occur; cache entry removed immediately"
    },
    {
      "scenario": "Very large timeout value",
      "input": "getOrCompute with timeout=Duration.ofDays(1)",
      "expected_output": "orTimeout waits up to 86,400,000ms before triggering timeout exception"
    },
    {
      "scenario": "Task function null reference",
      "input": "getOrCompute(key, null, timeout)",
      "expected_output": "supplyAsync(() -> null.apply(key)) throws NullPointerException during execution"
    }
  ]
}
```
